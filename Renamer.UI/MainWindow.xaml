<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:Renamer="clr-namespace:Renamer;assembly=Renamer" mc:Ignorable="d" x:Class="Renamer.UI.MainWindow"
        Title="MainWindow" Height="350" Width="525" Loaded="Window_Loaded">
   <Window.Resources>
      <CollectionViewSource x:Key="showDirectoryViewSource" d:DesignSource="{d:DesignInstance {x:Type Renamer:ShowDirectory}, CreateList=True}"/>
   </Window.Resources>
   <Grid DataContext="{StaticResource showDirectoryViewSource}">
      <ListView x:Name="showDirectoryListView" ItemsSource="{Binding}" Margin="20,5,97,114" SelectionMode="Single" HorizontalContentAlignment="Stretch">
         <ListView.ItemTemplate>
            <DataTemplate>
               <StackPanel Orientation="Vertical" Width="auto" Background="Beige">
                  <TextBlock Text="{Binding ShowNameOnDisk}" FontSize="14" FontWeight="Bold"></TextBlock>
                  <TextBlock Text="{Binding Location}" FontSize="10"></TextBlock>
               </StackPanel>
            </DataTemplate>
         </ListView.ItemTemplate>
      </ListView>
      <Button Content="Button" HorizontalAlignment="Left" Margin="341,240,0,0" VerticalAlignment="Top" Width="75" Click="Button_Click"/>
   </Grid>
</Window>
